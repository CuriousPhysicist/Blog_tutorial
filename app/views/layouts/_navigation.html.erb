<nav class="navbar navbar-default">
	<div class="container-fluid">
		<!-- Brand and toggle grouped together for better mobile performance -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-colapse" aria-expanded="false">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="#">Curious Blog</a>
		</div>
		<div class="collapse navbar-collapse" id="navbar-collapse">
			<ul class="nav navbar-nav navbar-right">
				<li><%= link_to "Home", root_path %></li>
				<% if admin_signed_in? %>
					<!-- This block is only visible to signed in administrators -->
					<li> <%= link_to "New Post", new_post_path %></li>
					<li> <%= link_to "My Account", edit_admin_registration_path %> </li>
					<li> <%= link_to "Logout", destroy_admin_session_path, :method => :delete %></li>
					<!-- the block ends here -->
				<% else %>
					<li> <%= link_to "Login", new_admin_session_path %> </li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>